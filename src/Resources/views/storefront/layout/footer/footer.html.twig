{% sw_extends "@Storefront/storefront/layout/footer/footer.html.twig" %}

{% block layout_footer_payment_shipping_logos %}
    {% block layout_battron_footer_payment_shipping_logos %}
            {% set config = page.footer.extensions.BattronFooterIcons %}
            {% set numberWords = ['One', 'Two', 'Three', 'Four', 'Five', 'Six', 'Seven', 'Eight', 'Nine', 'Ten'] %}

            {% block layout_footer_payment_logos %}
                <div class="col-md-6 footer-column js-footer-column">
                    <div class="footer-column-headline footer-headline js-footer-column-headline js-collapse-footer-column-trigger text-primary"
                         id="collapseFooterPaymentTitle"
                         data-target="#collapseFooterPayment"
                         aria-expanded="true"
                         aria-controls="collapseFooterPayment">
                            Einfach Bezahlen
                        <div class="footer-column-toggle">
                            <span class="footer-plus-icon">
                                {% sw_icon 'plus' %}
                            </span>
                            <span class="footer-minus-icon">
                                {% sw_icon 'minus' %}
                            </span>
                        </div>
                    </div>
                    <div id="collapseFooterPayment"
                         class="footer-column-content   js-footer-column-content footer-payment"
                         aria-labelledby="collapseFooterPaymentTitle">
                        <div class="row gy-3 "> <!-- Added gy-3 class for vertical spacing -->
                            {% for row in 0..2 %}
                                <div class="row g-2">
                                    {% for column in 0..2 %}
                                        {% set i = row * 3 + column %}
                                        {% set logoUrl = config['imgArray']['paymentLogo' ~ numberWords[i]] %}
                                        {% set link = config['paymentLink' ~ numberWords[i]] %}
                                        {% if logoUrl and link %}
                                            <div class="col d-flex justify-content-center align-items-center">
                                                <a href="{{ link }}">
                                                    <img src="{{ logoUrl }}" alt="Payment Logo {{ numberWords[i] }}" class="img-fluid w-50">
                                                </a>
                                            </div>
                                        {% endif %}
                                    {% endfor %}
                                </div>
                            {% endfor %}
                        </div>
                    </div>
                </div>
            {% endblock %}

            {% block layout_footer_shipping_logos %}
                <div class="col-md-6 footer-column js-footer-column">
                    <div class="footer-column-headline footer-headline js-footer-column-headline js-collapse-footer-column-trigger text-primary"
                         id="collapseFooterShippingTitle"
                         data-target="#collapseFooterShipping"
                         aria-expanded="true"
                         aria-controls="collapseFooterShipping">
                            Professioneller Versand
                        <div class="footer-column-toggle">
                            <span class="footer-plus-icon">
                                {% sw_icon 'plus' %}
                            </span>
                            <span class="footer-minus-icon">
                                {% sw_icon 'minus' %}
                            </span>
                        </div>
                    </div>
                    <div id="collapseFooterShipping"
                         class="footer-column-content  js-footer-column-content footer-shipping"
                         aria-labelledby="collapseFooterShippingTitle">
                        {% for row in 0..2 %}
                            <div class="row g-2">
                                {% for column in 0..2 %}
                                    {% set i = row * 3 + column %}
                                    {% set logoUrl = config['imgArray']['shippingLogo' ~ numberWords[i]] %}
                                    {% set link = config['shippingLink' ~ numberWords[i]] %}
                                    {% if logoUrl and link %}
                                        <div class="col d-flex justify-content-center align-items-center">
                                            <a href="{{ link }}">
                                                <img src="{{ logoUrl }}" alt="Shipping Logo {{ numberWords[i] }}" class="img-fluid" style="width: 40%;">
                                            </a>
                                        </div>
                                    {% endif %}
                                {% endfor %}
                            </div>
                        {% endfor %}
                    </div>
                </div>
            {% endblock %}
    {% endblock %}
{% endblock %}
